@model IEnumerable<StudyControlWeb.Models.DBO.Faculty>
@{
    
}
<div class="container">
    <div class="col p-3">
        <a class="btn btn-success" asp-action="CreateFaculty" asp-controller="Faculty">Добавить факультет</a>
    </div>
    <h3 class="p-3 text-center">Список факультетов</h3>
    
    <table class="table table-hover">
        <thead class="thead-dark">
        <tr>
            <th>Название</th>
            <th></th>
            <th></th>
        </tr>
        </thead>
        <tbody>
            @foreach(var f in Model)
            {
                <tr>
                    <td>@f.Title</td>
                    <td>
                        <a class="btn btn-secondary" asp-action="EditFaculty" asp-controller="Faculty" asp-route-id="@f.Id">Изменить</a>
                    </td>
                    <td>
                        <form asp-action="DeleteFaculty" asp-controller="Faculty" asp-route-id="@f.Id" method="post">
                            <input type="submit" class="btn btn-danger" value="Удалить"/>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
